language: python

cache:
        pip: true

before_install:
        - pip install codecov
        - pip install invoke

install:
        - invoke install

script:
        - invoke test

jobs:
        include:
                - python: "3.6.5"
                - python: "3.7.1"
                  dist: xenial

after_success:
        - codecov

notification:
        email: false
